# DISABLED — replaced by ios-deploy.yml
# This file kept for reference only
name: iOS CI (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use ios-deploy.yml instead"
